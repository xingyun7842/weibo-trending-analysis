name: Weibo Trending Analysis

on:
  # 定时执行：每天 UTC 时间 2:00 (北京时间 10:00)
  schedule:
    - cron: '0 2 * * *'

  # 手动触发
  workflow_dispatch:
    inputs:
      max_topics:
        description: '分析热点数量'
        required: false
        default: '15'

jobs:
  analyze-trends:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # 允许推送生成的报告

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/requirements.txt

      - name: Run Weibo Analysis with Claude Agent SDK
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL }}
          TIANAPI_KEY: ${{ secrets.TIANAPI_KEY }}
          MAX_TOPICS: ${{ github.event.inputs.max_topics || '15' }}
        run: |
          python scripts/weibo_analysis_agent.py

      - name: Upload HTML Report as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: weibo-analysis-report-${{ github.run_number }}
          path: reports/*.html
          retention-days: 90

      - name: Commit and Push Report (Optional)
        if: success()
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add reports/*.html || true
          git commit -m "Add Weibo analysis report - $(date +'%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push || echo "Nothing to push"
        continue-on-error: true

      - name: Summary
        if: always()
        run: |
          echo "## 📊 微博热搜分析完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**执行时间**: $(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "**分析热点数**: ${{ github.event.inputs.max_topics || '15' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "查看生成的报告请访问 Actions Artifacts 下载" >> $GITHUB_STEP_SUMMARY
