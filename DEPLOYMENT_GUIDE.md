# 🚀 GitHub Actions 部署指南

从本地 Claude Code Skill 迁移到 GitHub Actions 云端定时执行的完整指南。

---

## 📋 目录

1. [前置要求](#前置要求)
2. [项目结构](#项目结构)
3. [步骤 1: 创建 GitHub 仓库](#步骤-1-创建-github-仓库)
4. [步骤 2: 配置 GitHub Secrets](#步骤-2-配置-github-secrets)
5. [步骤 3: 推送代码到 GitHub](#步骤-3-推送代码到-github)
6. [步骤 4: 验证和测试](#步骤-4-验证和测试)
7. [步骤 5: 查看分析结果](#步骤-5-查看分析结果)
8. [常见问题](#常见问题)

---

## 前置要求

### 必需账号

- ✅ **GitHub 账号** - 用于托管代码和运行 Actions
- ✅ **Anthropic 账号** - 获取 Claude API 密钥
- ✅ **TianAPI 账号** - 获取微博热搜 API 密钥（可选，有默认密钥）

### 本地工具

- ✅ **Git** - 版本控制工具
- ✅ **文本编辑器** - VS Code / Notepad++ 等

### API 密钥准备

在开始之前，请准备好以下 API 密钥：

1. **Anthropic API Key**
   - 访问: https://console.anthropic.com/
   - 创建 API Key
   - 格式: `sk-ant-api03-xxxxx...`

2. **TianAPI Key**（可选）
   - 访问: https://www.tianapi.com/
   - 注册并申请"微博热搜榜" API
   - 默认密钥: `eda7b8c9c35234ce9a0dfd6939ae8c85`（共享，建议申请自己的）

---

## 项目结构

部署后的项目结构：

```
自动运行skill/
├── .github/
│   └── workflows/
│       └── weibo-analysis.yml          # GitHub Actions 工作流
├── scripts/
│   ├── weibo_analysis_agent.py         # 主分析脚本（Claude SDK）
│   └── requirements.txt                # Python 依赖
├── reports/                            # 生成的报告目录（自动创建）
├── .claude/                            # 原 Skill 配置（保留）
├── .gitignore                          # Git 忽略文件
├── SECRETS_SETUP.md                    # Secrets 配置指南
├── DEPLOYMENT_GUIDE.md                 # 本文档
└── README.md                           # 项目说明
```

---

## 步骤 1: 创建 GitHub 仓库

### 1.1 在 GitHub 创建新仓库

1. 访问 https://github.com/new
2. 填写仓库信息：
   - **Repository name**: `weibo-trending-analysis`（或自定义名称）
   - **Description**: `微博热搜产品创意自动分析系统 - GitHub Actions 版`
   - **Visibility**:
     - 🔒 **Private**（推荐）- 保护 API 使用记录
     - 🌍 **Public** - 开源分享
   - ❌ **不要**勾选 "Initialize this repository with a README"
3. 点击 **Create repository**

### 1.2 记录仓库地址

创建成功后，复制仓库 URL：
```
https://github.com/你的用户名/weibo-trending-analysis.git
```

---

## 步骤 2: 配置 GitHub Secrets

### 2.1 进入 Secrets 设置

1. 打开你的 GitHub 仓库页面
2. 点击 **Settings** 标签
3. 在左侧菜单找到 **Secrets and variables** → **Actions**
4. 点击 **New repository secret**

### 2.2 添加 ANTHROPIC_API_KEY

1. **Name**: `ANTHROPIC_API_KEY`
2. **Secret**: 粘贴你的 Claude API 密钥（`sk-ant-api03-...`）
3. 点击 **Add secret**

### 2.3 添加 TIANAPI_KEY

1. 点击 **New repository secret**
2. **Name**: `TIANAPI_KEY`
3. **Secret**: 粘贴你的 TianAPI 密钥
   - 使用默认: `eda7b8c9c35234ce9a0dfd6939ae8c85`
   - 或使用自己申请的密钥
4. 点击 **Add secret**

### 2.4 验证配置

确认 Secrets 列表显示：
- ✅ `ANTHROPIC_API_KEY`
- ✅ `TIANAPI_KEY`

**注意**: Secrets 值在保存后不可查看，只能更新或删除。

---

## 步骤 3: 推送代码到 GitHub

### 3.1 初始化 Git 仓库（如果尚未初始化）

在项目目录打开终端/命令提示符：

```bash
cd "C:\Users\admin\Desktop\自动运行skill"
git init
```

### 3.2 创建 .gitignore 文件

创建 `.gitignore` 文件，避免提交敏感信息：

```bash
# 在项目根目录创建 .gitignore
```

内容如下：

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# 敏感文件
*.env
.env.local
config.json
*.key
*.pem

# 报告文件（可选：如果不想提交报告到仓库）
reports/*.html

# 日志文件
*.log
*.jsonl

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 临时文件
*.tmp
*.bak
```

### 3.3 添加文件到 Git

```bash
# 添加所有文件
git add .

# 查看将要提交的文件
git status
```

### 3.4 创建首次提交

```bash
git commit -m "Initial commit: Weibo trending analysis with GitHub Actions"
```

### 3.5 关联远程仓库

```bash
# 替换为你的仓库 URL
git remote add origin https://github.com/你的用户名/weibo-trending-analysis.git
```

### 3.6 推送到 GitHub

```bash
# 推送到 main 分支
git branch -M main
git push -u origin main
```

**如果遇到认证问题**:
- 使用 Personal Access Token (PAT) 代替密码
- 配置 SSH 密钥

---

## 步骤 4: 验证和测试

### 4.1 检查文件上传

1. 刷新你的 GitHub 仓库页面
2. 确认以下文件已上传：
   - ✅ `.github/workflows/weibo-analysis.yml`
   - ✅ `scripts/weibo_analysis_agent.py`
   - ✅ `scripts/requirements.txt`
   - ✅ `SECRETS_SETUP.md`
   - ✅ `DEPLOYMENT_GUIDE.md`

### 4.2 手动触发第一次运行

1. 在仓库页面，点击 **Actions** 标签
2. 在左侧选择 **Weibo Trending Analysis** workflow
3. 点击右侧 **Run workflow** 按钮
4. （可选）修改 `max_topics` 参数（默认 15）
5. 点击绿色 **Run workflow** 按钮

### 4.3 查看运行日志

1. 等待几秒，刷新页面
2. 点击最新的 workflow run
3. 点击 **analyze-trends** job
4. 展开各个步骤查看日志：
   - ✅ `Set up Python` - Python 环境配置
   - ✅ `Install dependencies` - 安装依赖
   - ✅ `Run Weibo Analysis` - 执行分析（这一步最长，约 1-2 分钟）
   - ✅ `Upload HTML Report` - 上传报告

### 4.4 检查运行状态

- ✅ **绿色勾号** - 运行成功
- ❌ **红色叉号** - 运行失败，点击查看错误日志
- 🟡 **黄色圆点** - 正在运行中

---

## 步骤 5: 查看分析结果

### 方式 1: 下载 Artifacts（推荐）

1. 在 workflow run 页面，向下滚动到 **Artifacts** 部分
2. 点击下载 `weibo-analysis-report-{run_number}`
3. 解压 ZIP 文件
4. 在浏览器中打开 `.html` 文件

### 方式 2: 查看仓库中的报告（如果启用了提交）

1. 回到仓库主页
2. 进入 `reports/` 目录
3. 点击最新的 `.html` 文件
4. 点击 **Raw** 按钮查看原始 HTML
5. 复制内容到本地 `.html` 文件，在浏览器打开

### 方式 3: 查看 Summary

1. 在 workflow run 页面顶部
2. 查看 **Summary** 部分
3. 显示基本统计信息

---

## 🎯 配置定时运行

### 修改运行时间

编辑 `.github/workflows/weibo-analysis.yml` 文件：

```yaml
on:
  schedule:
    # 默认: 每天 UTC 2:00 (北京时间 10:00)
    - cron: '0 2 * * *'

    # 示例: 每天北京时间 18:00 (UTC 10:00)
    # - cron: '0 10 * * *'

    # 示例: 每 6 小时一次
    # - cron: '0 */6 * * *'

    # 示例: 每周一 9:00
    # - cron: '0 1 * * 1'
```

**Cron 语法说明**:
```
* * * * *
│ │ │ │ │
│ │ │ │ └─ 星期 (0-7, 0 和 7 都表示周日)
│ │ │ └─── 月份 (1-12)
│ │ └───── 日期 (1-31)
│ └─────── 小时 (0-23)
└───────── 分钟 (0-59)
```

**时区**: GitHub Actions 使用 UTC 时间
- 北京时间 = UTC + 8
- 北京 10:00 = UTC 02:00

### 提交修改

```bash
git add .github/workflows/weibo-analysis.yml
git commit -m "Update cron schedule"
git push
```

---

## 🔧 高级配置

### 修改分析参数

编辑 `scripts/weibo_analysis_agent.py`：

```python
# 在文件顶部修改
MAX_TOPICS = int(os.getenv("MAX_TOPICS", "15"))  # 分析数量
MIN_SCORE_EXCELLENT = 80                          # 优秀标准
MIN_SCORE_GOOD = 60                               # 良好标准
```

或在 workflow 文件中设置环境变量：

```yaml
env:
  MAX_TOPICS: '20'
  MIN_SCORE_EXCELLENT: '85'
  MIN_SCORE_GOOD: '65'
```

### 启用邮件通知

在 workflow 文件末尾添加：

```yaml
- name: Send Email Notification
  if: always()
  uses: dawidd6/action-send-mail@v3
  with:
    server_address: smtp.gmail.com
    server_port: 465
    username: ${{ secrets.EMAIL_USERNAME }}
    password: ${{ secrets.EMAIL_PASSWORD }}
    subject: 微博热搜分析报告 - ${{ job.status }}
    body: |
      分析已完成，状态: ${{ job.status }}
      查看结果: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
    to: your-email@example.com
    from: GitHub Actions
```

需要添加 Secrets:
- `EMAIL_USERNAME`
- `EMAIL_PASSWORD`

---

## 常见问题

### Q1: Workflow 没有自动运行

**可能原因**:
- Cron 时间未到
- 仓库没有活动（GitHub 可能禁用不活跃仓库的定时任务）

**解决方案**:
- 手动触发一次 workflow
- 检查 Actions 是否被禁用（Settings → Actions → General）

### Q2: "ANTHROPIC_API_KEY not set" 错误

**原因**: Secrets 配置不正确

**解决方案**:
1. 检查 Secret 名称拼写（区分大小写）
2. 重新创建 Secret
3. 确认 workflow 文件中环境变量引用正确

### Q3: Claude API 调用失败

**可能原因**:
- API 密钥无效
- 账户余额不足
- API 速率限制

**解决方案**:
1. 验证 API 密钥有效性
2. 检查 Anthropic 账户余额
3. 减少运行频率

### Q4: 报告生成但内容为空

**可能原因**:
- TianAPI 返回数据为空
- 网络问题
- API 配额用尽

**解决方案**:
1. 检查 TianAPI 账户状态
2. 查看 workflow 日志中的错误信息
3. 手动测试 API: `curl "https://apis.tianapi.com/weibohot/index?key=YOUR_KEY"`

### Q5: Git 推送失败（认证错误）

**解决方案**:

**方式 1: 使用 Personal Access Token (推荐)**
1. 访问 https://github.com/settings/tokens
2. 点击 **Generate new token** → **Classic**
3. 勾选 `repo` 权限
4. 生成并复制 token
5. 使用 token 作为密码推送：
   ```bash
   git push https://YOUR_USERNAME:YOUR_TOKEN@github.com/YOUR_USERNAME/REPO.git
   ```

**方式 2: 配置 SSH**
1. 生成 SSH 密钥: `ssh-keygen -t ed25519 -C "your_email@example.com"`
2. 添加到 GitHub: Settings → SSH and GPG keys
3. 更改远程地址:
   ```bash
   git remote set-url origin git@github.com:YOUR_USERNAME/REPO.git
   ```

### Q6: 如何查看历史报告？

**方式 1: Artifacts**
- 每次运行的报告都保存为 Artifact
- 保留 90 天（可在 workflow 中修改）
- Actions 页面 → 选择 run → 下载 Artifacts

**方式 2: Git 提交**
- 如果启用了自动提交，报告会保存在 `reports/` 目录
- 可通过 Git 历史查看所有报告

---

## 🎉 完成！

恭喜！你已经成功将 Claude Code Skill 迁移到 GitHub Actions。

### 下一步建议

1. ⭐ **Star 你的仓库** - 方便以后查找
2. 📧 **配置邮件通知** - 及时获取分析结果
3. 📊 **定期查看报告** - 发现产品创意机会
4. 🔄 **优化配置** - 根据需求调整参数
5. 🚀 **分享给团队** - 让更多人受益

### 技术架构总结

```
┌─────────────────────────────────────────────────────────┐
│                    GitHub Actions                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Cron Trigger (定时执行)                          │  │
│  └───────────────┬───────────────────────────────────┘  │
│                  ↓                                       │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Python Script (weibo_analysis_agent.py)          │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  1. Fetch Trends (TianAPI)                  │  │  │
│  │  │  2. Analyze with Claude (Anthropic SDK)     │  │  │
│  │  │  3. Generate HTML Report                    │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────┬───────────────────────────────────┘  │
│                  ↓                                       │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Outputs                                          │  │
│  │  • Artifact (HTML Report)                         │  │
│  │  • Git Commit (reports/*.html)                    │  │
│  │  • Summary (GitHub UI)                            │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

**文档版本**: 1.0
**最后更新**: 2026-01-20
**维护者**: Claude Code Assistant
**反馈**: 如有问题请提 Issue
