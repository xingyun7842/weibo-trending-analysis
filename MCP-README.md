# MCP Servers 使用手册

## 📋 概述

本文档列出了已安装和推荐的 Model Context Protocol (MCP) servers，包括每个服务的用途、主要功能和安全注意事项。

---

## 🔧 已安装的 MCP Servers

### 1. **context7**
**包名**: `@upstash/context7-mcp`
**状态**: ✅ 已安装

#### 📖 用途
上下文管理服务，用于在多次对话间保存和检索上下文信息。基于 Upstash 云存储。

#### 🎯 主要功能
- 跨会话保存上下文数据
- 智能检索相关历史信息
- 云端同步，支持多设备访问
- 语义搜索能力

#### ⚠️ 典型风险
- **隐私风险**: 数据存储在第三方云服务上，可能包含敏感信息
- **依赖性**: 需要网络连接和 Upstash 服务可用
- **API 限制**: 免费版可能有存储和请求限制
- **数据泄露**: 如果 API key 泄露，可能导致数据被未授权访问

#### 💡 使用建议
- 避免存储敏感的个人信息或密钥
- 定期审查存储的上下文内容
- 使用环境变量管理 Upstash API 密钥

---

### 2. **browser-mcp**
**包名**: `@browser-mcp/mcp-server`
**状态**: ✅ 已安装

#### 📖 用途
浏览器自动化服务，用于控制真实浏览器进行网页操作、数据抓取和自动化测试。

#### 🎯 主要功能
- 启动和控制浏览器实例
- 网页导航和交互（点击、输入、滚动）
- 网页截图和 PDF 生成
- JavaScript 执行
- Cookie 和存储管理

#### ⚠️ 典型风险
- **资源消耗**: 浏览器进程占用大量内存和 CPU
- **安全风险**: 执行任意 JavaScript 可能导致 XSS 攻击
- **网络暴露**: 浏览器可能访问恶意网站或泄露本地信息
- **进程泄露**: 未正确关闭的浏览器进程可能持续运行
- **反爬虫**: 可能被目标网站检测和封禁

#### 💡 使用建议
- 使用无头模式（headless）以节省资源
- 设置合理的超时时间
- 对访问的 URL 进行白名单验证
- 定期清理浏览器缓存和 Cookie

---

### 3. **fetch**
**包名**: `@modelcontextprotocol/server-fetch`
**状态**: ✅ 已配置

#### 📖 用途
HTTP 客户端服务，用于发送 HTTP/HTTPS 请求，获取远程资源和调用 API。

#### 🎯 主要功能
- GET/POST/PUT/DELETE 等 HTTP 方法支持
- 自定义请求头和查询参数
- JSON/XML/文本响应解析
- 超时和重试机制
- 代理支持

#### ⚠️ 典型风险
- **SSRF 攻击**: 可能被用于扫描内网或访问本地服务
- **数据泄露**: 请求可能包含敏感数据（API key、token）
- **DDoS 风险**: 过度请求可能导致目标服务不可用
- **恶意内容**: 获取的内容可能包含恶意代码或病毒
- **证书验证**: 关闭 SSL 验证可能导致中间人攻击

#### 💡 使用建议
- 限制可访问的域名（白名单机制）
- 使用 HTTPS 并验证 SSL 证书
- 设置合理的超时时间（避免长时间等待）
- 避免在 URL 中直接包含敏感信息
- 实施请求频率限制

---

### 4. **filesystem**
**包名**: `@modelcontextprotocol/server-filesystem`
**状态**: ✅ 已配置
**工作目录**: `/c/Users/admin/Desktop/自动运行skill`

#### 📖 用途
文件系统操作服务，提供读取、写入、创建、删除文件和目录的能力。

#### 🎯 主要功能
- 读取文件内容（文本、JSON、二进制）
- 写入和追加文件
- 创建、删除、重命名文件/目录
- 文件搜索和列表
- 文件元数据查询（大小、修改时间等）
- 路径解析和规范化

#### ⚠️ 典型风险
- **数据丢失**: 误删除或覆盖重要文件
- **权限提升**: 可能访问或修改系统敏感文件
- **路径遍历**: 通过 ../ 等手段访问限制目录外的文件
- **磁盘填满**: 大量写入操作可能耗尽磁盘空间
- **恶意文件**: 创建可执行文件或病毒
- **符号链接**: 通过符号链接绕过目录限制

#### 💡 使用建议
- **严格限制工作目录范围**（已配置为当前项目目录）
- 实施文件大小限制
- 禁止访问敏感系统目录（/etc, /sys, C:\Windows 等）
- 对文件路径进行严格验证和清理
- 重要操作前进行备份
- 避免创建可执行文件

---

### 5. **memory**
**包名**: `@modelcontextprotocol/server-memory`
**状态**: ✅ 已配置

#### 📖 用途
本地内存存储服务，在会话间保存键值对数据，用于存储配置、状态和临时数据。

#### 🎯 主要功能
- 键值对存储（set/get/delete）
- 数据持久化到本地文件
- 命名空间支持
- 批量操作
- 数据过期机制（TTL）
- JSON 数据结构支持

#### ⚠️ 典型风险
- **数据泄露**: 本地存储文件可能被其他程序读取
- **明文存储**: 敏感数据可能以明文形式存储
- **磁盘空间**: 无限制写入可能耗尽磁盘
- **数据污染**: 错误的数据可能影响后续操作
- **并发冲突**: 多个进程同时访问可能导致数据不一致

#### 💡 使用建议
- 不要存储明文密码或 API 密钥
- 定期清理过期数据
- 对存储文件设置适当的权限（600）
- 敏感数据使用加密存储
- 实施存储配额限制

---

### 6. **puppeteer**
**包名**: `@modelcontextprotocol/server-puppeteer`
**状态**: ✅ 已配置

#### 📖 用途
高级浏览器自动化服务，基于 Chromium，提供比 browser-mcp 更强大的控制能力。

#### 🎯 主要功能
- 完整的 Chrome DevTools Protocol 支持
- 网络请求拦截和修改
- 高级选择器（CSS、XPath、文本）
- 表单自动填充
- 文件上传/下载
- 模拟移动设备
- 性能监控和追踪
- PDF 打印和截图

#### ⚠️ 典型风险
- **高资源消耗**: Chromium 进程非常消耗内存
- **代码注入**: 可执行任意 JavaScript 代码
- **Cookie 劫持**: 可能获取和泄露登录凭证
- **指纹识别**: 浏览器指纹可能暴露用户身份
- **下载风险**: 自动下载可能包含恶意文件
- **反爬虫**: 高级网站可能检测并封禁自动化工具

#### 💡 使用建议
- 使用沙箱模式运行
- 限制可访问的域名
- 定期清理下载目录
- 使用代理隐藏真实 IP
- 模拟人类行为（随机延迟、鼠标移动）
- 监控内存使用，及时关闭页面

---

### 7. **sequential-thinking**
**包名**: `@modelcontextprotocol/server-sequential-thinking`
**状态**: ✅ 已配置

#### 📖 用途
复杂任务规划和执行服务，将大型任务分解为多个步骤，按顺序执行并追踪进度。

#### 🎯 主要功能
- 任务分解和规划
- 步骤依赖关系管理
- 执行进度追踪
- 错误处理和重试
- 任务状态持久化
- 并行任务支持

#### ⚠️ 典型风险
- **无限循环**: 错误的任务依赖可能导致死循环
- **资源耗尽**: 大量并行任务可能耗尽系统资源
- **执行失败**: 某个步骤失败可能导致整个任务失败
- **时间过长**: 复杂任务可能运行很长时间
- **状态不一致**: 中断可能导致任务状态混乱

#### 💡 使用建议
- 设置合理的任务超时时间
- 限制最大任务深度和并行数
- 实施检查点机制，支持断点续传
- 定期清理已完成或失败的任务
- 监控任务执行状态

---

### 8. **sqlite**
**包名**: `@modelcontextprotocol/server-sqlite`
**状态**: ✅ 已配置
**数据库路径**: `/c/Users/admin/Desktop/自动运行skill/data.db`

#### 📖 用途
本地 SQLite 数据库服务，提供结构化数据存储、查询和管理能力。

#### 🎯 主要功能
- SQL 查询执行（SELECT, INSERT, UPDATE, DELETE）
- 表结构创建和修改
- 事务支持
- 索引管理
- 全文搜索
- 数据导入/导出
- 备份和恢复

#### ⚠️ 典型风险
- **SQL 注入**: 未参数化的查询可能被注入恶意 SQL
- **数据丢失**: 错误的删除或更新操作
- **锁定问题**: 长时间事务可能锁定数据库
- **磁盘空间**: 大量数据写入可能填满磁盘
- **性能问题**: 复杂查询可能导致高 CPU 使用
- **并发冲突**: SQLite 对并发写入支持有限

#### 💡 使用建议
- **始终使用参数化查询**，避免 SQL 注入
- 定期备份数据库文件
- 对敏感数据列进行加密
- 使用事务处理批量操作
- 建立适当的索引以提高查询性能
- 限制查询结果集大小
- 设置数据库文件权限为 600

---

### 9. **brave-search**
**包名**: `@modelcontextprotocol/server-brave-search`
**状态**: ✅ 已配置（需要 API Key）

#### 📖 用途
集成 Brave Search API，提供网络搜索能力，获取实时网络信息。

#### 🎯 主要功能
- 网络搜索查询
- 新闻搜索
- 图片搜索
- 安全搜索过滤
- 地理位置过滤
- 搜索结果排名
- 相关搜索建议

#### ⚠️ 典型风险
- **API 配额**: 免费 API 有请求次数限制
- **密钥泄露**: API key 泄露可能被滥用
- **隐私问题**: 搜索查询可能被记录
- **恶意内容**: 搜索结果可能包含恶意链接
- **费用**: 超出免费额度可能产生费用
- **速率限制**: 过度请求可能被暂时封禁

#### 💡 使用建议
- **必须配置 BRAVE_API_KEY 环境变量**
- 使用环境变量而非硬编码存储 API key
- 实施查询缓存以减少 API 调用
- 监控 API 使用量
- 对搜索结果进行安全过滤
- 避免在查询中包含敏感信息
- 获取 API Key: https://brave.com/search/api/

---

## 🚀 使用指南

### 启动 MCP Servers

MCP servers 会在 Claude Code 启动时自动加载。配置文件位置：
```
~/.config/claude/claude_desktop_config.json
```

### 重新加载配置

修改配置后，需要重启 Claude Code 以加载新的 MCP servers：
```bash
# 退出当前会话
exit

# 重新启动
claude-code
```

### 验证 MCP 状态

可以通过以下方式检查 MCP 是否正常运行：
```bash
# 查看 MCP 日志目录
ls ~/AppData/Local/claude-cli-nodejs/Cache/*/mcp-logs-*

# 查看特定 MCP 的日志
tail -f ~/AppData/Local/claude-cli-nodejs/Cache/*/mcp-logs-fetch/stdio.log
```

---

## 🔐 安全最佳实践

### 1. 最小权限原则
- 只安装必需的 MCP servers
- 限制 filesystem 的工作目录范围
- 使用白名单限制 fetch 可访问的域名

### 2. 密钥管理
- 使用环境变量存储 API 密钥
- 不要在代码或配置文件中硬编码密钥
- 定期轮换 API 密钥

### 3. 数据保护
- 不要在 memory 或 context7 中存储敏感信息
- 对 SQLite 数据库中的敏感数据进行加密
- 定期清理临时文件和缓存

### 4. 网络安全
- 验证 SSL 证书
- 使用 HTTPS 而非 HTTP
- 对外部输入进行严格验证

### 5. 资源管理
- 设置合理的超时时间
- 监控内存和磁盘使用
- 及时清理未使用的浏览器进程

### 6. 审计和监控
- 定期查看 MCP 日志
- 监控异常活动（大量请求、错误等）
- 记录关键操作

---

## 🛠️ 故障排查

### MCP 无法启动

**问题**: MCP server 启动失败
**解决**:
1. 检查 npm/npx 是否正常工作：`npx --version`
2. 查看 MCP 日志文件查找错误信息
3. 手动安装 MCP 包：`npm install -g @modelcontextprotocol/server-fetch`
4. 确保 Node.js 版本 >= 18

### API Key 错误

**问题**: brave-search 报告 API key 无效
**解决**:
1. 确认已在配置中设置 `BRAVE_API_KEY`
2. 检查 API key 是否正确（无多余空格）
3. 确认 API key 未过期或超出配额
4. 从 Brave Search API 网站重新获取密钥

### 文件系统权限错误

**问题**: filesystem MCP 无法读写文件
**解决**:
1. 检查工作目录路径是否正确
2. 确认 Claude Code 进程有目录访问权限
3. 在 Windows 上检查目录是否被锁定或只读
4. 使用绝对路径而非相对路径

### 浏览器启动失败

**问题**: puppeteer 或 browser-mcp 无法启动浏览器
**解决**:
1. 确保系统已安装 Chrome/Chromium
2. 检查是否有足够的内存
3. 尝试使用无头模式
4. 查看浏览器进程是否有残留（任务管理器）
5. 手动安装 Chromium：`npx puppeteer browsers install chrome`

---

## 📚 参考资源

- [Model Context Protocol 官方文档](https://modelcontextprotocol.io/)
- [Claude Code 文档](https://docs.anthropic.com/claude/claude-code)
- [MCP Servers 官方仓库](https://github.com/modelcontextprotocol/servers)
- [Brave Search API 文档](https://brave.com/search/api/)
- [Upstash Context7 文档](https://upstash.com/docs/context7)

---

## 📝 配置文件位置

- **主配置**: `~/.config/claude/claude_desktop_config.json`
- **日志目录**: `~/AppData/Local/claude-cli-nodejs/Cache/*/mcp-logs-*`
- **SQLite 数据库**: `/c/Users/admin/Desktop/自动运行skill/data.db`
- **Filesystem 工作目录**: `/c/Users/admin/Desktop/自动运行skill`

---

## 🎯 使用场景示例

### 微博热点分析（你的当前场景）

推荐 MCP 组合：
- **fetch**: 调用微博 API 获取热点数据
- **filesystem**: 保存分析结果为 HTML/JSON
- **sqlite**: 存储历史热点数据
- **memory**: 缓存配置和中间结果
- **sequential-thinking**: 规划多步分析流程

### 数据爬取与处理

推荐 MCP 组合：
- **puppeteer**: 处理需要 JavaScript 渲染的页面
- **fetch**: 调用 REST API
- **sqlite**: 存储爬取的数据
- **filesystem**: 保存图片和文件

### 自动化任务

推荐 MCP 组合：
- **sequential-thinking**: 任务规划
- **memory**: 存储任务状态
- **filesystem**: 读写配置文件
- **brave-search**: 获取实时信息

---

**文档版本**: 1.0
**最后更新**: 2026-01-17
**维护者**: Claude Code Assistant

